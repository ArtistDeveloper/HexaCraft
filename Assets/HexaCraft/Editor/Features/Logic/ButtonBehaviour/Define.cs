using UnityEngine;

namespace HexaCraft
{
    public enum ToggleButton
    {
        [SceneRegistration(true)]
        MaterialChange,

        [SceneRegistration(true)]
        ObjectSelection,
    }

    public enum Button
    {
        GridGeneration,
    }
}
